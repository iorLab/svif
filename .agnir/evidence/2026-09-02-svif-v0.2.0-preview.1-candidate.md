# Svif v0.2.0-preview.1 Repository Preview candidate — 2026-09-02

## Candidate scope

- product/Plugin version: `0.2.0-preview.1`
- portable contracts: unchanged `0.2` line
- package: Skills-only Plugin; no MCP/App increment
- shared workflow: `plugin/skills/svif/SKILL.md`
- supported candidate acceptance surfaces: Codex CLI and ChatGPT desktop/Codex
- distribution: `.agents/plugins/marketplace.json` -> `./plugin`
- staging branch: `release/svif-v0.2.0-preview.1`
- authoritative repository/ref: `iorLab/svif` / `main`

The release branch is target-main staging only. It does not become a second continuity authority.

## User entry and installer boundary

The user-facing install intent remains:

```text
Install and enable Svif for this Project: https://github.com/iorLab/svif
```

The prompt carries no version, command, bootstrap checklist, or client caveat. The installer owns immutable Preview resolution, marketplace registration, supported-surface checks, first-use Agnir bootstrap, and truthful failure reporting. A released Preview must resolve to its immutable tag; moving `main` is maintainer development only.

## Static verification

- repository integrity: pass
- portable contracts: pass
- focused Plugin package/distribution/installation suite: 31 tests passed
- full Svif runtime/unit suite: 72 tests passed
- Skill quick validation: pass
- Plugin manifest, Codex manifest, `VERSION`, and active Cloudflare adapter version agree on `0.2.0-preview.1`
- both README languages carry the same short install intent and separate Repository Preview from universal-directory publication
- initial candidate commit: `1dbcce6582f218b0762fb655bc03455517c79802`
- GitHub pull request: `https://github.com/iorLab/svif/pull/2`
- GitHub Actions run: `33590346950`; `repository-integrity`, `portable-contracts`, and `runtime-kernel` succeeded

## Supported-client candidate acceptance

The repository marketplace was fixed to commit `1dbcce6582f218b0762fb655bc03455517c79802`. Both clients exposed Svif `0.2.0-preview.1` as installed and enabled from that marketplace revision.

### Codex CLI

- observed CLI: `codex-cli 0.151.0-alpha.7.2`
- isolated `CODEX_HOME` and isolated Git Project used; no existing user Plugin state was reused
- the exact one-line user intent caused `svif@svif` to install and enable from the pre-registered immutable marketplace snapshot
- ordinary non-Agnir Project bootstrap created one stable Project identity, `AGNIR.yaml`, `.agnir/`, `SVIF.yaml`, the minimal `AGENTS.md` locator, and README Agnir instructions
- a subsequent fresh CLI session loaded the installed Skill, completed the recorded `RESULT.md` task, verified exact bytes, and checkpointed state/evidence
- another fresh CLI session recovered the completed state without prior chat context and reported no remaining action
- an initialized Project retained byte-identical `AGENTS.md`, `AGNIR.yaml`, README, and `SVIF.yaml`; its existing identity `urn:uuid:11111111-1111-4111-8111-111111111111` remained unchanged while the recorded task and checkpoint advanced
- a fresh initialized-Project session independently recovered and verified that completed state

### ChatGPT desktop/Codex

- desktop installation task: `01a06061-7578-7c10-8a70-0ebccea66d2c`
- the task received only the exact one-line user intent; the global marketplace had been pre-registered to the immutable candidate SHA but Svif was not preinstalled
- the desktop client installed and enabled `svif@svif` `0.2.0-preview.1`, bootstrapped Project identity `urn:uuid:baf08aa3-c050-42da-b213-ba1f35369a7e`, and validated package revision, activation, binding, and cold-start Agnir loading
- a subsequent Project task created `RESULT.md` with exact bytes `svif preview desktop accepted\n`, SHA-256 `25d8d691335a865c5e0187cab415dfcea1c48ea4d790d94b928e6af868bd416d`, then checkpointed state/evidence
- independent recovery task `01a06068-4472-7530-a8ef-bedfd9fdbfa4` read the original Project root without prior chat context, verified the same result/hash and installed/enabled Plugin state, and made no writes
- the desktop task environment reported an older `codex --version` value (`0.147.0`) than the invoking CLI; this did not change the exposed Plugin version, immutable marketplace SHA, or acceptance result

The acceptance fixtures contain no protected external effects, credentials, or capability grants. Client warnings from an unrelated unauthenticated Cloudflare MCP startup were observed in CLI sessions but did not affect the Skills-only Plugin path or its verification results.

## Final candidate boundary

Recording this evidence changes the branch tip but not the Plugin tree, manifests, product code, contracts, or installation instructions. The resulting evidence-only candidate commit must receive green CI and an immutable-ref installation smoke before it may advance `main`.

## Explicitly pending

- final evidence-only candidate commit SHA, remote CI, and immutable-ref smoke
- authoritative-main integration
- immutable `v0.2.0-preview.1` tag and GitHub Prerelease
- tag-based installation smoke and post-release checkpoint
- universal Plugins Directory submission/publication and personal ChatGPT Web validation

No pending layer is represented as completed evidence.
