# Agnir v0.1.0 compatible upgrade — 2026-09-01

## Classification

Svif was already a valid Agnir Project using Core `0.1` and discovery profile `repository-filesystem/0.1`. The target Agnir stable release `v0.1.0` uses the same compatibility lines, so this operation is a **compatible operational upgrade**, not a migration.

## Target

- source: `iorLab/agnir`
- stable release: `0.1.0`
- Git tag: `v0.1.0`
- immutable applied revision: `2a0cb7bf2068b11f361e315670b2f2dc497b2588`

The tag was independently verified to resolve to that exact revision and the GitHub Release exists as a non-draft, non-prerelease stable release.

## Preservation

The upgrade preserves:

- Project identity `urn:svif:project:svif-core`;
- Agnir Core compatibility `0.1`;
- discovery profile `repository-filesystem/0.1`;
- all existing memory locators and durable `.agnir/` continuity;
- Svif `SVIF.yaml` compatibility/bindings;
- unrelated README and `AGENTS.md` content.

## Applied operational changes

- `AGNIR.yaml` records `extensions.agnir/operations` provenance for Agnir `0.1.0` and its immutable source revision.
- canonical README `Agnir Project Instructions` is refreshed to the v0.1.0 transactional checkpoint and repository commit/push boundary semantics.
- the Simplified Chinese parallel README is kept behaviorally synchronized.
- stale continuity wording that described Agnir `0.1.0` as pending publication is corrected.

## Completion gate

This candidate upgrade must be published as one coherent Svif repository revision, then verified by fresh activation from the Svif Project root and the repository's normal CI checks. Exact resulting revision/run identifiers are external observations and are not embedded self-referentially in this candidate evidence.
