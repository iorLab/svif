# Authoritative Svif Agnir Core/profile 0.2 → 1.0 promotion — 2026-09-07

Status: **accepted on authoritative `main`.**

## Authorization and normative contract

The Principal explicitly authorized the Svif Project to perform the separately governed Agnir Core/profile `0.2` → `1.0` promotion on 2026-09-07.

Normative promotion contract: `iorLab/agnir/spec/CORE_0_2_TO_1_0_PROMOTION.md`.

The contract requires explicit authorization, exact source capture, preservation of Project identity/lineage/continuity semantics and locators, complete staging before publication, stale-source rejection, coherent authoritative publication, fresh 1.0 discovery and idempotent compatibility semantics.

## Captured source

- authoritative Svif source revision: `a00ad6ed9f18abddbed1979a619f932350cefe54`;
- source Agnir Core/profile: `0.2` / `repository-filesystem/0.2`;
- source Agnir operational provenance: `v0.2.0@fc84095ed5d500be9e1b43a4af0e93356571bbd4`;
- Project identity: `urn:svif:project:svif-core`;
- authoritative logical lineage: `urn:svif:lineage:authoritative`;
- VCS selector: `refs/heads/main`;
- durable locators: `.agnir/state.md`, `.agnir/next-actions.md`, `.agnir/decisions.md`, `.agnir/evidence/`.

The staged promotion branch was `promotion/agnir-1.0`.

## Implementation pressure and repaired staging

The first staging run `34075847299` successfully constructed the intended target and passed the repository binding guard, but full unittest discovery exposed one stale current-binding assertion in `tests/test_plugin_package.py` that still required Core/profile `0.2`. No candidate was published from that failed run.

A repair attempt run `34075919475` correctly produced a local workflow repair, but GitHub rejected the Actions-token push because the token could not update another workflow file without workflow permission. No product candidate was published from that attempt either.

The stager was then repaired through the repository connector, preserving the exact source guard and adding the missed current-binding test update. This was tooling/staging repair, not a weakening of promotion semantics or conformance.

## Complete staged candidate

Successful staging run: `34076031454`.

The run required `origin/main` and the promotion merge-base to remain exactly `a00ad6ed9f18abddbed1979a619f932350cefe54`, then constructed and verified the complete candidate.

Final candidate head: `5a88eee4bc214b8da3b4c5f48640067b167248b8`.

Candidate changes:

- `AGNIR.yaml`: Core `1.0`, profile `repository-filesystem/1.0`, operational release `1.0.0`, applied revision `6d16dcfd17b8e9f22fd25804e22b9f8a516d06c3`;
- `SVIF.yaml`: Agnir continuity compatibility/profile `1.0` / `repository-filesystem/1.0` while Svif version and Project Binding remain `0.2`;
- `src/svif/continuity/agnir.py`: stable 1.0 dispatch added while 0.1/0.2 remain supported;
- `checks/check_repository.py`: 1.0 binding and lineage/selector validation added;
- adapter/current-self-consumption/Plugin tests extended to 1.0;
- shared Svif Skill updated to recognize supported 1.0 repository/filesystem Projects and the repository's current 1.0 self-host binding;
- exhaustive repository tree updated for the new candidate evidence and 1.0 adapter/test responsibilities.

The candidate intentionally did **not** modify `.agnir/state.md`, `.agnir/next-actions.md`, `.agnir/decisions.md`, brand assets, or `VERSION` during staging.

## Preservation result

The complete candidate preserved:

- Project identity `urn:svif:project:svif-core`;
- logical lineage `urn:svif:lineage:authoritative`;
- VCS selector/binding `refs/heads/main`;
- State / Next Actions / Decisions / Evidence locators;
- Project policy and unrelated extensions;
- Svif product line `0.2`;
- `project-binding/0.2`;
- brand identity and production assets;
- immutable released Preview `v0.2.0-preview.1`;
- released Preview.1 first-use bootstrap Core/profile `0.1` baseline.

The immutable Agnir target provenance is `v1.0.0@6d16dcfd17b8e9f22fd25804e22b9f8a516d06c3`.

## Candidate and synthetic integration verification

Staging run `34076031454` passed:

- exact-source freshness guard;
- preservation/target-shape guard;
- repository integrity;
- full unittest discovery;
- portable conformance.

Draft PR #9 was opened from `promotion/agnir-1.0` to `main`.

PR synthetic-merge product-check run `34076166897` passed all three canonical jobs:

- repository-integrity: success;
- portable-contracts: success;
- runtime-kernel: success.

Immediately before publication, authoritative `main` was freshly re-read and remained exactly `a00ad6ed9f18abddbed1979a619f932350cefe54`; PR #9 was mergeable. No stale-source reconciliation was required.

## Authoritative publication

PR #9 was marked ready and squash-merged with expected head SHA locked to `5a88eee4bc214b8da3b4c5f48640067b167248b8`.

Authoritative promotion commit:

`5da0eb76e38e817ba0f5111ce2b08750afa3b9c3`

Post-publication main push run `34076249501` passed all canonical Svif product checks.

Fresh authoritative reads after publication confirmed:

- `AGNIR.yaml`: Core/profile `1.0` / `repository-filesystem/1.0`;
- Agnir operational provenance: `v1.0.0@6d16dcfd17b8e9f22fd25804e22b9f8a516d06c3`;
- `SVIF.yaml`: continuity compatibility/profile `1.0` / `repository-filesystem/1.0`;
- Svif version: `0.2`;
- Project Binding: `project-binding/0.2`;
- Project identity, logical lineage, selector and durable locators: unchanged.

The annotated Svif tag `v0.2.0-preview.1` still resolves to released commit `2b07b6b5ea0bc8feee59f9f647be9af3069d056e`; no tag was moved.

## Temporary branch retirement

The completed promotion branch final tip `5a88eee4bc214b8da3b4c5f48640067b167248b8` was recorded in `history/BRANCH_ARCHIVE.md` at commit `cf72584c2848eb1c4073ad7a811218e0a4b73d1b`.

One-shot retirement workflow creation commit: `51a3648f284c361347aea822b3ae6b5a245719aa`.

Retirement workflow run `34076414723`: success.

The workflow deleted `promotion/agnir-1.0`, removed itself, and advanced `main` to cleanup revision `9710102fc7db0ed40f5876ba356264088a04f796`. Fresh ref lookup returns 404 for the promotion branch and the one-shot workflow path is absent from `main`.

## Result

Svif is now a real downstream Project operating on Agnir Core/profile `1.0` while preserving historical 0.1/0.2 compatibility in its adapter and preserving its immutable Preview.1 first-use Core/profile `0.1` release boundary.

The promotion was explicit, semantics-preserving, stale-guarded, coherently published and freshly verified. No Agnir 1.0 promotion gate remains open.
