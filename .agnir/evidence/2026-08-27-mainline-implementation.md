# Evidence — Svif 0.2 main-line implementation start

Date: 2026-08-27

Implemented on active `main`:

- direct Svif `0.2` repository descriptor;
- Agnir `0.1` self-hosted continuity through `AGNIR.yaml` + `.agnir/`;
- normative Svif Core, Capability Adapter, Evidence, and Software Delivery Profile documents;
- machine-readable Capability Adapter and Evidence Record JSON Schemas;
- executable Svif structural conformance checker and CI workflow;
- removal of active ZeroLocal v0.1 specification, draft, Skill, and old conformance files from `main`.

The predecessor remains preserved on `legacy/zerolocal-v0.1` and is not relabeled as Svif conformance.
