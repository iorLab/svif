# Predecessor lineage

Svif's predecessor ZeroLocal v0.1 is recorded at boundary commit `8ccbb1d30520ca3d0b8b9f2cfe2963d35a853cf6`.

The former branch `legacy/zerolocal-v0.1` was retired under main-only branch governance. Historical recovery uses the boundary commit, Git history, and `history/BRANCH_ARCHIVE.md`; no legacy branch is required.

That lineage contains the ZeroLocal v0.1 specification, `ZL-*` conformance, ChatGPT Core/Cloudflare Provider Skills, validation records, recovery playbook, and the predecessor repository-memory layout.

The active `main` line intentionally does not carry those mutable predecessor structures. Historical behavior and evidence remain recoverable through Git history.

Validation Project #1 remains historical ZeroLocal v0.1 evidence; it is not relabeled as Svif conformance. Validation Project #2 must be redefined against the Svif/Agnir contracts before resuming.
